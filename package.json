{
  "name": "amp",
  "version": "0.1.0",
  "private": true,
  "description": "Ara GitHub org profile and shared tooling",
  "scripts": {
    "lint": "markdownlint '**/*.md' --ignore node_modules",
    "lint:fix": "markdownlint '**/*.md' --ignore node_modules --fix",
    "test": "echo 'No tests defined yet' && exit 0",
    "validate": "node scripts/validate-profile.js"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.39.0"
  },
  "engines": {
    "node": ">=18"
  }
}
