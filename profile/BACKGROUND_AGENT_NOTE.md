# Background agent note

Requested task: stop Review pane flicker in `OnboardingWizardState.swift`.

This worktree is checked out from `github.com/Aradotso/.github` and only contains the organization profile assets under `profile/`:

- `profile/README.md`
- `profile/hero.png`
- `profile/logo.png`

The requested Swift source file is not present in this repository/worktree, so no app code or end-to-end onboarding test could be performed here. This note is committed so the branch can be published and picked up in the correct `ara-cua` worktree.
