# Ara

Ara is an agent-building agent — design, run, and refine automated agents directly on your Mac.
It ships as a native macOS app backed by a cloud platform (auth, billing, hosted runtime, voice relay).
